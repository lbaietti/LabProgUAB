#ifndef GEOMTRIA_H
#define GEOMETRIA_H


/**
 * @param raio entra como parâmetro para que possa ser retornado o tamanho do círculo
 */
double areaCirculo(double raio);

/**
 * @param base, altura - entram como parâmetros para que possa ser retornado o tamanho do retângulo
 */
double areaRetangulo(double base, double altura);

#endif